import toPrecision, {ROUNDING_DEFAULT, ROUNDING_FIXED, ROUNDING_SIGNIFICANT} from './to-precision';

console.warn('"pretty-num/src/reduce-precision" is deprecated, use "pretty-num/src/to-precision" instead');

export default toPrecision;

export {
    ROUNDING_DEFAULT,
    ROUNDING_SIGNIFICANT,
    ROUNDING_FIXED,
};
